**Prompt :**

"Créer une image pédagogique et visuellement attrayante sur l’utilisation des sous-modules Git. L’image doit contenir les sections suivantes, chacune avec un titre concis et une représentation visuelle :

1. **Ajouter un Sous-module** : Afficher la commande `git submodule add <URL> <chemin>` avec une icône représentant la structure d’un dépôt Git, mettant en évidence l’ajout d’un sous-module au sein d’un dépôt principal.

2. **Mettre à Jour un Sous-module** : Afficher la commande `git submodule update --remote` avec une illustration de rafraîchissement ou de synchronisation, symbolisant la mise à jour d’un sous-module avec ses dernières modifications.

3. **Supprimer un Sous-module** : Présenter la séquence de commandes pour supprimer un sous-module (`git config`, `git rm --cached`, et `rm -rf <chemin>`), accompagnée d’un symbole de suppression, montrant le retrait d’un sous-module de la hiérarchie.

4. **Cloner avec les Sous-modules** : Montrer la commande `git clone --recurse-submodules <URL>`, avec une icône de clonage d’un dépôt contenant plusieurs sous-dépôts (sous-modules).

5. **Cloner sans Initialiser les Sous-modules** : Illustrer le clonage avec `git clone <URL>` et l’option d’initialisation ultérieure avec `git submodule update --init <chemin>`, indiquant que les sous-modules peuvent être ajoutés plus tard.

Chaque section doit avoir une couleur de fond ou un cadre distinct pour les séparer clairement. Utiliser des icônes ou des illustrations simples liées à Git, au code, aux dépôts et aux dossiers pour rendre chaque commande intuitive."

---

Faites-moi savoir si vous souhaitez ajouter d'autres détails ou sections dans ce prompt !